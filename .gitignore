# Gitignore for the member gateway template.
# This repo intentionally tracks .member-env.nix (placeholder values) so the
# flake can evaluate. EDIT it in place with YOUR values — do not push real
# values back upstream; keep them in your own fork/clone.

result
/nix